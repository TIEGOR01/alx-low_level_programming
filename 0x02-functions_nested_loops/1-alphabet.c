#include "main.h"
/**
 * main - alphabet, in lowercase
 * print_alphabet - Make the alphabet
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		_putchar(c);
	}
	_putchar('\n');
}
