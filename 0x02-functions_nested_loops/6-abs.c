#include "main.h"

/**
 * main - Function that computes the absolute value of an integer
 *
 * Return: Absolute value
 */
int _abs(int)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
	_putchar ('\n');
}
